---

cert_domains:
  - domain: "octopi.{{ common_local_tld }}"
    method: --dns dns_cf
    script: "octopi.sh"
    script_args: "{{ config_directory }}/certs/octopi.{{ common_local_tld }}/fullchain.cer {{ config_directory }}/certs/octopi.{{ common_local_tld }}/octopi.{{ common_local_tld }}.key"
