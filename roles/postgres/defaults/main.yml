---

postgres_name: postgres
postgres_version: '12'
postgres_image: "postgres:{{ postgres_version }}-alpine"
postgres_ports: []
postgres_directory: /tmp/postgres
postgres_environment_variables:
  POSTGRES_PASSWORD: mysecretpassword
  POSTGRES_USER: postgres
  POSTGRES_DB: mydb
