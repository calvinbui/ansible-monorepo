---

pip_install_packages:
  - name: virtualenv
    executable: pip3
  - name: pyyaml
    executable: pip3
    state: present
  - name: awscli
    executable: pip3
    state: absent
  - name: requests
    virtualenv: /tmp/venv
  - name: pylint
    version: 1.9.4
    executable: pip3
