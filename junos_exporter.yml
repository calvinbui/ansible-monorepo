---

- hosts: homelab

  vars:
    application: junos_exporter

    docker_network: "{{ networks.user }}"

  handlers:
    - name: Restart
      ansible.builtin.command: docker restart "{{ application }}"

  tasks:
    - name: Create config folder
      ansible.builtin.file:
        path: "{{ config_directory }}"
        state: directory
        owner: "{{ common_user }}"
        group: "{{ common_group }}"
        mode: "0771"
      register: _config_dir

    - name: Create config file
      ansible.builtin.copy:
        dest: "{{ _config_dir.path }}/config.yaml"
        mode: "755"
        content: |
          ---
          configs:
            default:
              username: root
              password: {{ _junos_password }}
              enabled_collectors:
                - interface
                - environment
                - route_engine
          global:
            timeout: 30
      vars:
        _junos_password: !vault |
          $ANSIBLE_VAULT;1.1;AES256
          
          6134
      notify: Restart

    - name: Create container
      ansible.builtin.include_role:
        name: docker_container
      vars:
        image: tynany/junos_exporter:latest
        volumes:
          - "{{ _config_dir.path }}/config.yaml:/config.yaml"
