---

- hosts: homelab

  vars:
    application: privatebin

    docker_network: "{{ networks.pub }}"

  handlers:
    - name: Restart
      ansible.builtin.command: docker restart "{{ application }}"

  tasks:
    - name: Create config folders
      ansible.builtin.file:
        path: "{{ config_directory }}"
        state: directory
        mode: "0700"
        owner: nobody
        group: "82"  # nobody in alpine

    - name: Create folders
      ansible.builtin.file:
        path: "{{ config_directory }}/{{ item }}"
        state: directory
        mode: "0700"
        owner: nobody
        group: "82"  # nobody in alpine
      loop:
        - cfg
        - data

    - name: Install jmespath locally
      ansible.builtin.pip:
        name: jmespath
        executable: pip3
      delegate_to: localhost
      become: false
      become_user: "{{ common_user }}"

    - name: Set URL shortener value
      ansible.builtin.set_fact:
        _url_shortener_value: "https://{{ common_short_tld }}/api/v2/action/shorten?key={{ _url_shortener_api_key }}&response_type=json&url="
      vars:
        _url_shortener_api_key: !vault |
          $ANSIBLE_VAULT;1.1;AES256
          

    - name: Template config
      ansible.builtin.template:
        src: "{{ files_directory }}/conf.php.j2"
        dest: "{{ config_directory }}/cfg/conf.php"
        mode: "0644"
        owner: nobody
        group: "82"
      notify: Restart

    - name: Create container
      ansible.builtin.include_role:
        name: docker_container
      vars:
        image: privatebin/nginx-fpm-alpine
        volumes:
          - "{{ config_directory }}/data:/srv/data"
          - "{{ config_directory }}/cfg:/srv/cfg"
        traefik:
          - port: 8080
        homer:
          name: PrivateBin
          service: Sharing
          priority: 790
          subtitle: "Zero knowledge pastebin"
