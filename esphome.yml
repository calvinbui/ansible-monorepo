---

- hosts: homelab

  vars:
    application: esphome

    # the key is a 32-byte base64 encoded string. provide the base64 encoded value when authenticating
    esphome_password: !vault |
      $ANSIBLE_VAULT;1.1;AES256
      
      6235383039663730666262393961303832653539616363633630

    docker_network: "{{ networks.iot }}"

  handlers:
    - name: Restart
      community.docker.docker_container:
        name: "{{ application }}"
        restart: true
        comparisons:
          '*': ignore

  tasks:
    - name: Create config folder
      ansible.builtin.file:
        path: "{{ config_directory }}"
        state: directory
        owner: "{{ common_user }}"
        group: "{{ common_group }}"
        mode: "0771"

    - name: Create configs
      ansible.builtin.template:
        src: "{{ item }}"
        dest: "{{ config_directory }}/{{ item | basename | regex_replace('\\.j2$', '') }}"
        mode: "0755"
        owner: "{{ common_root_id }}"
        group: "{{ common_root_group }}"
      with_fileglob:
        - "{{ files_directory }}/*"
        - "{{ files_directory }}/.*" # dotfiles are hidden from the dashboard
      notify: Restart

    - name: Create container
      ansible.builtin.include_role:
        name: docker_container
      vars:
        image: ghcr.io/esphome/esphome:2023.2.4
        env:
        volumes:
          - "{{ config_directory }}:/config"
          - /etc/localtime:/etc/localtime:ro
        privileged: true
        network_mode: "host"
        traefik:
          - port: 6052
            auth: page
        homer:
          name: ESPHome
          service: Home
          priority: 98
          subtitle: "Control ESP8266/ESP32"
